# 주문 모드 레이어 색상 저장 누락 수정

- **날짜**: 2026-03-26 14:28
- **관련 주문**: ORD-20260325-PG7CLU
- **증상**: 색상을 화이트로 지정한 주문인데 목업이 블랙으로 표시됨. 에디터에서 컬러칩으로 화이트 변경 시 목업에 즉시 반영되지만, 저장 후 새로고침하면 컬러칩은 화이트인데 목업만 블랙으로 되돌아감.

## 원인

`saveOrderMode`(주문 모드 저장 함수)가 `canvas_state`에 `productColor`만 저장하고 **`layerColors`(멀티 레이어 제품의 레이어별 색상)를 저장하지 않았음.**

멀티 레이어 제품은 `productColor`가 아닌 `layerColors`로 각 레이어의 색상을 관리하는데, 이 값이 `canvas_state`에 포함되지 않아 새로고침 시 `layer.colorOptions[0]`(첫 번째 색상 옵션 = 블랙)으로 폴백됨.

컬러칩 UI(`OrderModePanel`)는 `canvas_state.productColor`를 읽어 표시하므로 화이트로 정상 표시되지만, 캔버스 렌더링은 `layerColors`를 참조하므로 불일치 발생.

참고: `saveDesignMode`와 `saveTemplateMode`는 이미 `layerColors`를 포함하고 있어 이 문제가 없었음. `saveOrderMode`만 누락.

## 수정 파일 및 내용

### 1. `components/editor/hooks/useEditorSave.ts` (핵심 수정)

**수정 위치**: `saveOrderMode` 함수 내 `updatedCanvasState[side.id]` 생성부

**Before**:
```typescript
updatedCanvasState[side.id] = {
  ...existingState,
  objects: serializedObjects,
  productColor,
};
```

**After**:
```typescript
updatedCanvasState[side.id] = {
  ...existingState,
  objects: serializedObjects,
  productColor,
  layerColors: layerColors[side.id] || {},
};
```

`layerColors[side.id]`를 `canvas_state`에 함께 저장하여, 새로고침 후에도 레이어별 색상이 유지되도록 함.

### 2. `components/canvas/SingleSideCanvas.tsx` (폴백 개선)

**수정 위치**: 레이어 초기 색상 결정 로직 (초기화 시 `initialColor` 계산부)

**Before**:
```typescript
const initialColor = (typeof initialLayerColors[layer.id] === 'string' && ...)
  ? (initialLayerColors[layer.id] as string)
  : layer.colorOptions[0]?.hex || '#FFFFFF';
```

**After**:
```typescript
const parsedProductColor = /* canvasState에서 productColor 추출 */;
const initialColor = (typeof initialLayerColors[layer.id] === 'string' && ...)
  ? (initialLayerColors[layer.id] as string)
  : parsedProductColor || layer.colorOptions[0]?.hex || '#FFFFFF';
```

`layerColors` 없이 저장된 기존 주문 데이터에 대해, `productColor`가 존재하면 이를 레이어 색상 폴백값으로 사용. 기존에는 곧바로 `colorOptions[0]`(블랙 가능)으로 폴백했으나, 이제 `productColor`(화이트 등)를 중간 폴백으로 사용.

### 3. `components/OrderCanvasRenderer.tsx` (폴백 개선)

**수정 위치**: 읽기 전용 렌더러의 멀티 레이어 색상 결정부

**Before**:
```typescript
const selectedColor = state?.layerColors?.[layer.id] || layer.colorOptions[0]?.hex || '#FFFFFF';
```

**After**:
```typescript
const selectedColor = state?.layerColors?.[layer.id] || state?.productColor || layer.colorOptions[0]?.hex || '#FFFFFF';
```

`OrderCanvasRenderer`(주문 상세 등에서 사용하는 읽기 전용 캔버스)에도 동일한 폴백 로직 적용.

## 영향 범위

- 이후 주문 모드에서 저장하는 모든 주문 항목의 `canvas_state`에 `layerColors`가 포함됨
- 기존에 `layerColors` 없이 저장된 주문은 `productColor` 폴백으로 블랙 대신 올바른 색상 표시
- 단일 이미지 모드(레이어 없는 제품)는 기존과 동일하게 `productColor`만 사용하므로 영향 없음
