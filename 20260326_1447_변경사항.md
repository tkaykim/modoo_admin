# 변경사항 종합 기록

- **날짜**: 2026-03-26 14:47
- **커밋**: `947f487` (`fix: design-order sync, layer color save, build error fix`)
- **대상 저장소**: https://github.com/Peace-Corp/modoo_admin (main 브랜치)

---

## 변경 파일 요약

| # | 파일 | 유형 | 변경 내용 요약 |
|---|------|------|---------------|
| 1 | `app/api/admin/designs/[id]/sync-orders/route.ts` | **신규** | 디자인→주문 동기화 API |
| 2 | `components/editor/hooks/useEditorSave.ts` | **수정** | 저장 시 동기화 호출 + layerColors 저장 누락 수정 |
| 3 | `components/OrderCanvasRenderer.tsx` | **수정** | productColor 폴백 추가 |
| 4 | `components/canvas/SingleSideCanvas.tsx` | **수정** | productColor 폴백 추가 |
| 5 | `components/canvas/Toolbar.tsx` | **수정** | 다중 이미지 업로드 지원 리팩토링 |
| 6 | `issue-order-thumbnail-not-synced.md` | **신규** | 이슈 분석 문서 |
| 7 | `docs/FIX_DESIGN_ORDER_THUMBNAIL_SYNC.md` | **신규** | 동기화 구현 설명 문서 |
| 8 | `20260326_1428_주문모드_레이어색상_저장누락_수정.md` | **신규** | 레이어 색상 수정 기록 |
| 9 | `docs/20260326_1428_에디터_다중이미지_업로드_수정.md` | **신규** | 다중 업로드 수정 기록 |

---

## 1. 디자인-주문 동기화 API 신규 생성

### 파일: `app/api/admin/designs/[id]/sync-orders/route.ts` (신규)

### 왜 필요했는가

주문 생성 시 `saved_designs`의 데이터(`preview_url`, `canvas_state`, `color_selections`)가 `order_items`에 **복사**된다. 그런데 이후 관리자가 디자인을 수정해도 `order_items`는 갱신되지 않아, 주문 화면에서 수정 전 썸네일이 계속 표시되는 문제가 있었다.

### 어떻게 바뀌었는가

`POST /api/admin/designs/:designId/sync-orders` API 엔드포인트를 새로 만들었다.

- 관리자 인증 필수 (`profiles.role === 'admin'`)
- `saved_designs` 테이블에서 해당 디자인의 최신 `preview_url`, `canvas_state`, `color_selections` 조회
- `order_items` 테이블에서 `design_id`가 일치하는 모든 항목을 찾아 일괄 업데이트
- 응답: `{ data: { synced: number } }` (동기화된 항목 수 반환)

**동기화 매핑:**

| order_items 필드 | 원본 (saved_designs) |
|---|---|
| `thumbnail_url` | `preview_url` |
| `canvas_state` | `canvas_state` |
| `color_selections` | `color_selections` |
| `updated_at` | 현재 시각 |

### 빌드 에러 수정 (이 파일)

원래 코드에서 Supabase의 `.select()` 메서드에 2개 인수를 전달하여 TypeScript 타입 에러가 발생했다.

**Before (빌드 실패):**
```typescript
const { error: updateError, count } = await adminClient
  .from('order_items')
  .update({
    thumbnail_url: design.preview_url,
    canvas_state: design.canvas_state,
    color_selections: design.color_selections,
    updated_at: new Date().toISOString(),
  })
  .in('id', orderItemIds)
  .select('id', { count: 'exact', head: true });
//          ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^ ← 2번째 인수 불허
```

**After (빌드 성공):**
```typescript
const { error: updateError, count } = await adminClient
  .from('order_items')
  .update({
    thumbnail_url: design.preview_url,
    canvas_state: design.canvas_state,
    color_selections: design.color_selections,
    updated_at: new Date().toISOString(),
  }, { count: 'exact' })  // ← count 옵션을 update()로 이동
  .in('id', orderItemIds);
// .select() 제거 — count만 필요하므로 데이터 반환 불필요
```

**이유:** Supabase JS v2에서 `.select(columns)` 메서드는 0~1개 인수(컬럼 문자열)만 받는다. `count` 옵션은 `.update(values, options)` 의 두 번째 인수로 전달해야 한다.

---

## 2. 디자인 저장 시 자동 동기화 호출 + layerColors 저장 누락 수정

### 파일: `components/editor/hooks/useEditorSave.ts` (수정)

### 변경 A: 저장 후 자동 동기화 호출

**왜:** 위에서 만든 동기화 API를 관리자가 매번 수동으로 호출하는 것은 비현실적이다. 디자인 저장 시 자동으로 연관 주문 항목도 갱신되어야 한다.

**Before:**
```typescript
const updated = await updateDesign(savedDesign.id, updateData);
if (!updated) return { success: false, error: '디자인 업데이트에 실패했습니다.' };
return { success: true, id: updated.id };
```

**After:**
```typescript
const updated = await updateDesign(savedDesign.id, updateData);
if (!updated) return { success: false, error: '디자인 업데이트에 실패했습니다.' };

syncDesignToOrderItems(savedDesign.id);  // ← 비동기 호출 추가

return { success: true, id: updated.id };
```

**추가된 함수:**
```typescript
function syncDesignToOrderItems(designId: string) {
  fetch(`/api/admin/designs/${designId}/sync-orders`, { method: 'POST' })
    .then((res) => {
      if (!res.ok) console.warn('order_items sync failed:', res.status);
      return res.json();
    })
    .then((data) => {
      if (data?.data?.synced > 0) {
        console.log(`Synced ${data.data.synced} order item(s) with design ${designId}`);
      }
    })
    .catch((err) => console.warn('order_items sync error:', err));
}
```

- **fire-and-forget 방식**: 동기화 실패가 디자인 저장 자체를 실패시키지 않는다
- 새 디자인 생성 시에는 호출하지 않음 (아직 연결된 order_items가 없으므로)

### 변경 B: saveOrderMode에서 layerColors 저장 누락 수정

**왜:** 멀티 레이어 제품(예: 후드티 앞/뒤)의 레이어별 색상은 `layerColors`로 관리된다. `saveDesignMode`와 `saveTemplateMode`는 이 값을 `canvas_state`에 포함하고 있었지만, `saveOrderMode`만 빠져 있었다. 이로 인해 에디터에서 색상을 화이트로 변경해도 저장 후 새로고침하면 목업이 블랙(첫 번째 색상 옵션)으로 되돌아가는 현상이 발생했다.

**Before:**
```typescript
updatedCanvasState[side.id] = {
  ...existingState,
  objects: serializedObjects,
  productColor,
};
```

**After:**
```typescript
updatedCanvasState[side.id] = {
  ...existingState,
  objects: serializedObjects,
  productColor,
  layerColors: layerColors[side.id] || {},  // ← 추가
};
```

---

## 3. OrderCanvasRenderer에 productColor 폴백 추가

### 파일: `components/OrderCanvasRenderer.tsx` (수정, 1줄)

### 왜

주문 상세/목록 화면에서 사용하는 읽기 전용 캔버스 렌더러이다. 기존에 `layerColors` 없이 저장된 주문 데이터의 경우, `layerColors`가 없으면 곧바로 `colorOptions[0]`(보통 블랙)으로 폴백했다. `productColor`가 존재하면 이를 우선 사용하도록 중간 폴백을 추가했다.

**Before (781행):**
```typescript
const selectedColor = state?.layerColors?.[layer.id] || layer.colorOptions[0]?.hex || '#FFFFFF';
```

**After:**
```typescript
const selectedColor = state?.layerColors?.[layer.id] || state?.productColor || layer.colorOptions[0]?.hex || '#FFFFFF';
//                                                      ^^^^^^^^^^^^^^^^^^^^ 중간 폴백 추가
```

**폴백 우선순위:**
1. `layerColors[layer.id]` — 레이어별 개별 색상 (가장 정확)
2. `productColor` — 제품 전체 색상 (새로 추가된 폴백)
3. `colorOptions[0].hex` — 첫 번째 색상 옵션 (기존 폴백)
4. `#FFFFFF` — 최종 기본값

---

## 4. SingleSideCanvas에 productColor 폴백 추가

### 파일: `components/canvas/SingleSideCanvas.tsx` (수정, 395~401행)

### 왜

에디터 캔버스의 레이어 이미지 초기 색상 결정 로직이다. `OrderCanvasRenderer`와 동일한 문제가 있었다. `initialLayerColors`에 해당 레이어 색상이 없으면 `colorOptions[0]`으로 폴백하던 것을, `productColor`를 중간 폴백으로 추가했다.

**Before (395~397행):**
```typescript
const initialColor = (typeof initialLayerColors[layer.id] === 'string' && (initialLayerColors[layer.id] as string).startsWith('#'))
  ? (initialLayerColors[layer.id] as string)
  : layer.colorOptions[0]?.hex || '#FFFFFF';
```

**After:**
```typescript
const parsedProductColor = (() => {
  if (!canvasState) return null;
  const cs = typeof canvasState === 'string'
    ? (() => { try { return JSON.parse(canvasState); } catch { return null; } })()
    : canvasState;
  return typeof cs?.productColor === 'string' ? cs.productColor : null;
})();
const initialColor = (typeof initialLayerColors[layer.id] === 'string' && (initialLayerColors[layer.id] as string).startsWith('#'))
  ? (initialLayerColors[layer.id] as string)
  : parsedProductColor || layer.colorOptions[0]?.hex || '#FFFFFF';
//    ^^^^^^^^^^^^^^^^^^ canvasState에서 productColor를 안전하게 파싱하여 폴백으로 사용
```

`canvasState`가 문자열일 수도 객체일 수도 있으므로, 안전하게 파싱하여 `productColor`를 추출한다.

---

## 5. Toolbar — 다중 이미지 업로드 지원 리팩토링

### 파일: `components/canvas/Toolbar.tsx` (수정, 147 insertions, 164 deletions)

### 왜

사용자가 이미지를 여러 개 선택해도 에디터 캔버스에 첫 번째 파일만 추가되는 버그가 있었다.

**원인 2가지:**
1. `<input type="file">` 요소에 `multiple` 속성이 없어 파일 다중 선택 자체가 불가능했음
2. `onchange` 핸들러에서 `target.files?.[0]`으로 첫 번째 파일만 처리하고 나머지를 무시했음

### 어떻게 바뀌었는가

#### A. `addSingleImageToCanvas` 헬퍼 함수 추출

기존 `addImage` 내부에 인라인으로 작성되어 있던 단일 파일 처리 로직(업로드, AI/PSD 변환, 캔버스 추가)을 독립된 함수로 분리했다.

```typescript
const addSingleImageToCanvas = async (file: File, canvas: fabric.Canvas) => {
  // 1. AI/PSD 파일인 경우 → PNG 변환 → 원본 업로드 → PNG 업로드
  // 2. 일반 이미지인 경우 → 직접 업로드
  // 3. fabric.FabricImage 생성 → 크기 조정 → 캔버스 중앙 배치
  // 4. metadata(supabaseUrl, supabasePath 등) 첨부
};
```

#### B. 파일 input에 `multiple` 속성 추가

```typescript
// Before
input.accept = 'image/*,.ai,.psd';

// After
input.accept = 'image/*,.ai,.psd';
input.multiple = true;  // ← 다중 파일 선택 가능
```

#### C. onchange 핸들러 — 모든 파일 순차 처리

```typescript
// Before
const file = target.files?.[0];  // 첫 번째만
if (!file) return;
// ... 단일 파일 처리

// After
const files = target.files;
if (!files || files.length === 0) return;
const fileList = Array.from(files);
for (let i = 0; i < fileList.length; i++) {
  await addSingleImageToCanvas(fileList[i], canvas);  // 모든 파일 순차 처리
}
```

#### D. 로딩 모달 진행 상태 표시 개선

- 다중 파일 업로드 시 `(1/5) image1.png 처리 중...` 형식으로 진행 상황 표시
- 완료 시 `N개 파일이 추가되었습니다.` 메시지 표시
- 개별 파일 실패 시에도 나머지 파일은 계속 처리 (에러 로깅만 수행)

#### E. 불필요한 주석 제거

리팩토링 과정에서 `// Create a hidden file input element`, `// Trigger file input click` 등 코드 흐름을 단순 서술하는 주석들이 제거되었다.

---

## 6~9. 신규 문서 파일

| 파일 | 내용 |
|---|---|
| `issue-order-thumbnail-not-synced.md` | 주문 후 디자인 수정 시 썸네일 미반영 문제의 근본 원인 분석. order_items가 정적 스냅샷인 구조적 문제와 코드베이스 내 관련 코드 위치 정리 |
| `docs/FIX_DESIGN_ORDER_THUMBNAIL_SYNC.md` | 위 문제에 대한 해결 구현(sync-orders API + 자동 호출) 설명 |
| `20260326_1428_주문모드_레이어색상_저장누락_수정.md` | layerColors 저장 누락 버그의 증상, 원인, 수정 내용 기록 |
| `docs/20260326_1428_에디터_다중이미지_업로드_수정.md` | Toolbar 다중 이미지 업로드 버그의 원인과 수정 내용 기록 |

---

## 전체 동작 흐름 (수정 후)

```
관리자가 design mode에서 디자인 수정 → 저장 클릭
  ↓
useEditorSave.saveDesignMode()
  ↓
canvas_state에 productColor + layerColors 포함하여 저장
  ↓
updateDesign() → saved_designs UPDATE
  ↓
syncDesignToOrderItems() (fire-and-forget)
  ↓
POST /api/admin/designs/:id/sync-orders
  ↓
order_items WHERE design_id = :id
  → UPDATE thumbnail_url, canvas_state, color_selections, updated_at
  ↓
주문 화면에서 최신 썸네일/캔버스 표시
```

---

## 하위 호환성

- 기존에 `layerColors` 없이 저장된 주문 데이터 → `productColor` 폴백으로 올바른 색상 표시
- 단일 이미지 모드(레이어 없는 제품) → 기존과 동일하게 `productColor`만 사용
- order mode에서 주문 항목 직접 편집/저장하는 기존 로직 → 변경 없음
- 단일 파일 업로드 → 기존과 동일하게 동작 (multiple 속성만 추가, 로직 변경 없음)
